name: hello_world_dapp
description: Flutter and Blockchain

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5

  provider: ^6.0.3
  web3dart: ^2.3.5
  http: ^0.13.4
  web_socket_channel: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: any

flutter:
  uses-material-design: true

  assets:
    - src/artifacts/HelloWorld.json